export interface HeroConfig {
  /** Path from public/, e.g. "/images/hero.jpg". Empty → black background. */
  fluidImagePath: string
}

export const heroConfig: HeroConfig = {
  fluidImagePath: "/images/milk-hero.jpg",
}
